# Implementación Críticos 1-3 — Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Eliminar `src/Utility/`, unificar estados/prioridades de tickets en `src/Constants/`, consolidar tickets BD con estados legacy, y sincronizar `CLAUDE.md` con la realidad post-refactor.

**Architecture:** Refactor en 5 fases (A: crear Constants/, B: migrar consumidores, B.5: migration BD, C: eliminar referencias legacy, D: borrar Utility/) más actualización de CLAUDE.md al final. Cada fase deja el código compilable y ejecutable; rollback por `git revert` excepto B.5 (BD irreversible — requiere `mysqldump` previo).

**Tech Stack:** PHP 8.1+, CakePHP 5.x, MySQL/MariaDB, `composer cs-fix`/`cs-check`, `bin/cake migrations`. Sin suite de tests automatizada — verificación manual con smoke tests + `grep`.

**Spec base:** `docs/superpowers/specs/2026-05-07-criticos-1-3-design.md`.

**Correcciones aplicadas vs spec** (detectadas durante writing-plans):
- Colores en `TicketConstants::STATUS_COLORS` preservan los del código actual (`nuevo='#ffc107'` amarillo, `abierto='#dc3545'` rojo) en vez de los del spec — para que la UI no cambie visualmente.
- `TicketConstants::PRIORITY_COLORS::PRIORITY_ALTA` se mantiene como `'#ffc107'` (amarillo) según `StatusHelper`, no `'#fd7e14'` del spec.
- API de `StatusHelper` se preserva: métodos siguen siendo `statusLabel`/`statusColor`/`priorityLabel`/`priorityColor` (no se renombran).

---

## Fase A — Crear `src/Constants/` y mover trait (aditivo)

### Task 1: Tag de rollback y verificación pre-state

**Files:** ninguno (solo git).

- [ ] **Step 1: Verificar que git status está limpio**

Run: `git status`
Expected: rama `main`, working tree clean (o solo el spec ya commiteado).

Si hay cambios sin commitear, hacer commit o stash antes de seguir.

- [ ] **Step 2: Crear tag de rollback**

```bash
git tag pre-criticos-1-3
```

- [ ] **Step 3: Verificar tag creado**

Run: `git tag | grep pre-criticos-1-3`
Expected: imprime `pre-criticos-1-3`.

- [ ] **Step 4: Confirmar estructura inicial**

Run: `ls src/Utility/`
Expected:
```
SettingKeys.php
SettingsEncryptionTrait.php
ValidationConstants.php
```

Run: `ls src/Constants/ 2>&1`
Expected: `ls: cannot access 'src/Constants/': No such file or directory` (la carpeta NO existe aún).

---

### Task 2: Crear `src/Constants/SettingKeys.php`

**Files:**
- Create: `src/Constants/SettingKeys.php`

- [ ] **Step 1: Crear el directorio Constants**

```bash
mkdir -p src/Constants
```

- [ ] **Step 2: Crear archivo con contenido**

Create `src/Constants/SettingKeys.php`:

```php
<?php
declare(strict_types=1);

namespace App\Constants;

/**
 * Centralized setting key constants.
 *
 * Eliminates hardcoded setting key strings duplicated across services,
 * controllers, and commands.
 */
final class SettingKeys
{
    // ── System ──────────────────────────────────────────────────────────
    public const SYSTEM_TITLE = 'system_title';

    // ── Gmail ───────────────────────────────────────────────────────────
    public const GMAIL_REFRESH_TOKEN = 'gmail_refresh_token';
    public const GMAIL_CLIENT_SECRET_JSON = 'gmail_client_secret_json';
    public const GMAIL_CHECK_INTERVAL = 'gmail_check_interval';
    public const GMAIL_USER_EMAIL = 'gmail_user_email';

    // ── WhatsApp ────────────────────────────────────────────────────────
    public const WHATSAPP_ENABLED = 'whatsapp_enabled';
    public const WHATSAPP_API_URL = 'whatsapp_api_url';
    public const WHATSAPP_API_KEY = 'whatsapp_api_key';
    public const WHATSAPP_INSTANCE_NAME = 'whatsapp_instance_name';
    public const WHATSAPP_TICKETS_NUMBER = 'whatsapp_tickets_number';

    // ── n8n ─────────────────────────────────────────────────────────────
    public const N8N_ENABLED = 'n8n_enabled';
    public const N8N_WEBHOOK_URL = 'n8n_webhook_url';
    public const N8N_API_KEY = 'n8n_api_key';
    public const N8N_SEND_TAGS_LIST = 'n8n_send_tags_list';
    public const N8N_TIMEOUT = 'n8n_timeout';

    // ── Webhooks ────────────────────────────────────────────────────────
    public const WEBHOOK_GMAIL_IMPORT_TOKEN = 'webhook_gmail_import_token';
}
```

- [ ] **Step 3: Verificar sintaxis**

Run: `php -l src/Constants/SettingKeys.php`
Expected: `No syntax errors detected in src/Constants/SettingKeys.php`

---

### Task 3: Crear `src/Constants/RoleConstants.php`

**Files:**
- Create: `src/Constants/RoleConstants.php`

- [ ] **Step 1: Crear archivo con contenido**

Create `src/Constants/RoleConstants.php`:

```php
<?php
declare(strict_types=1);

namespace App\Constants;

/**
 * User role constants.
 */
final class RoleConstants
{
    public const ROLE_ADMIN             = 'admin';
    public const ROLE_AGENT             = 'agent';
    public const ROLE_SERVICIO_CLIENTE  = 'servicio_cliente';
    public const ROLE_REQUESTER         = 'requester';

    public const ROLES = [
        self::ROLE_ADMIN,
        self::ROLE_AGENT,
        self::ROLE_SERVICIO_CLIENTE,
        self::ROLE_REQUESTER,
    ];

    /** Roles con acceso al panel interno staff */
    public const STAFF_ROLES = [
        self::ROLE_ADMIN,
        self::ROLE_AGENT,
        self::ROLE_SERVICIO_CLIENTE,
    ];
}
```

- [ ] **Step 2: Verificar sintaxis**

Run: `php -l src/Constants/RoleConstants.php`
Expected: `No syntax errors detected`.

---

### Task 4: Crear `src/Constants/CacheConstants.php`

**Files:**
- Create: `src/Constants/CacheConstants.php`

- [ ] **Step 1: Crear archivo con contenido**

Create `src/Constants/CacheConstants.php`:

```php
<?php
declare(strict_types=1);

namespace App\Constants;

/**
 * Cache configuration constants.
 */
final class CacheConstants
{
    public const CACHE_SETTINGS = 'system_settings';
    public const CACHE_CONFIG   = '_cake_core_';

    public const DEFAULT_SYSTEM_TITLE = 'Mesa de Ayuda';
}
```

- [ ] **Step 2: Verificar sintaxis**

Run: `php -l src/Constants/CacheConstants.php`
Expected: `No syntax errors detected`.

---

### Task 5: Crear `src/Constants/TicketConstants.php`

**Files:**
- Create: `src/Constants/TicketConstants.php`

- [ ] **Step 1: Crear archivo con contenido**

Create `src/Constants/TicketConstants.php`:

```php
<?php
declare(strict_types=1);

namespace App\Constants;

/**
 * Ticket domain constants — single source of truth for status, priority,
 * comment types, and their presentation (labels, colors, icons).
 */
final class TicketConstants
{
    // ── Estados ───────────────────────────────────────────────────────
    public const STATUS_NUEVO     = 'nuevo';
    public const STATUS_ABIERTO   = 'abierto';
    public const STATUS_PENDIENTE = 'pendiente';
    public const STATUS_RESUELTO  = 'resuelto';

    public const STATUSES = [
        self::STATUS_NUEVO,
        self::STATUS_ABIERTO,
        self::STATUS_PENDIENTE,
        self::STATUS_RESUELTO,
    ];

    public const RESOLVED_STATUSES = [
        self::STATUS_RESUELTO,
    ];

    public const OPEN_STATUSES = [
        self::STATUS_NUEVO,
        self::STATUS_ABIERTO,
        self::STATUS_PENDIENTE,
    ];

    public const STATUS_LABELS = [
        self::STATUS_NUEVO     => 'Nuevo',
        self::STATUS_ABIERTO   => 'Abierto',
        self::STATUS_PENDIENTE => 'Pendiente',
        self::STATUS_RESUELTO  => 'Resuelto',
    ];

    public const STATUS_COLORS = [
        self::STATUS_NUEVO     => '#ffc107',
        self::STATUS_ABIERTO   => '#dc3545',
        self::STATUS_PENDIENTE => '#0d6efd',
        self::STATUS_RESUELTO  => '#198754',
    ];

    public const STATUS_ICONS = [
        self::STATUS_NUEVO     => 'bi-circle-fill',
        self::STATUS_ABIERTO   => 'bi-circle-fill',
        self::STATUS_PENDIENTE => 'bi-circle-fill',
        self::STATUS_RESUELTO  => 'bi-circle-fill',
    ];

    // ── Prioridades ───────────────────────────────────────────────────
    public const PRIORITY_BAJA    = 'baja';
    public const PRIORITY_MEDIA   = 'media';
    public const PRIORITY_ALTA    = 'alta';
    public const PRIORITY_URGENTE = 'urgente';

    public const PRIORITIES = [
        self::PRIORITY_BAJA,
        self::PRIORITY_MEDIA,
        self::PRIORITY_ALTA,
        self::PRIORITY_URGENTE,
    ];

    public const PRIORITY_LABELS = [
        self::PRIORITY_BAJA    => 'Baja',
        self::PRIORITY_MEDIA   => 'Media',
        self::PRIORITY_ALTA    => 'Alta',
        self::PRIORITY_URGENTE => 'Urgente',
    ];

    public const PRIORITY_COLORS = [
        self::PRIORITY_BAJA    => '#6c757d',
        self::PRIORITY_MEDIA   => '#0dcaf0',
        self::PRIORITY_ALTA    => '#ffc107',
        self::PRIORITY_URGENTE => '#dc3545',
    ];

    // ── Comment types ─────────────────────────────────────────────────
    public const COMMENT_PUBLIC   = 'public';
    public const COMMENT_INTERNAL = 'internal';
    public const COMMENT_SYSTEM   = 'system';

    /** Permitidos en input de usuario */
    public const COMMENT_TYPES = [self::COMMENT_PUBLIC, self::COMMENT_INTERNAL];

    /** Permitidos en BD (incluye los autogenerados por el sistema) */
    public const ALL_COMMENT_TYPES = [self::COMMENT_PUBLIC, self::COMMENT_INTERNAL, self::COMMENT_SYSTEM];
}
```

- [ ] **Step 2: Verificar sintaxis**

Run: `php -l src/Constants/TicketConstants.php`
Expected: `No syntax errors detected`.

---

### Task 6: Crear `src/Service/Traits/SettingsEncryptionTrait.php`

**Files:**
- Create: `src/Service/Traits/SettingsEncryptionTrait.php`

- [ ] **Step 1: Crear archivo con contenido**

Create `src/Service/Traits/SettingsEncryptionTrait.php`:

```php
<?php
declare(strict_types=1);

namespace App\Service\Traits;

use App\Constants\SettingKeys;
use Cake\Utility\Security;

/**
 * Settings Encryption Trait
 *
 * Provides automatic encryption/decryption for sensitive system settings.
 * Similar to password hashing for users, but reversible (two-way encryption).
 *
 * Usage:
 * - Use encryptSetting() when saving to database
 * - Use decryptSetting() when reading from database
 */
trait SettingsEncryptionTrait
{
    /**
     * List of setting keys that should be encrypted
     *
     * @var array
     */
    private array $encryptedSettings = [
        SettingKeys::GMAIL_REFRESH_TOKEN,
        SettingKeys::GMAIL_CLIENT_SECRET_JSON,
        SettingKeys::WHATSAPP_API_KEY,
        SettingKeys::N8N_API_KEY,
        SettingKeys::WEBHOOK_GMAIL_IMPORT_TOKEN,
    ];

    /**
     * Check if a setting key should be encrypted
     *
     * @param string $key Setting key
     * @return bool
     */
    protected function shouldEncrypt(string $key): bool
    {
        return in_array($key, $this->encryptedSettings, true);
    }

    /**
     * Encrypt a setting value
     *
     * @param string $value Plain text value
     * @param string $key Setting key (for context)
     * @return string Encrypted value
     */
    protected function encryptSetting(string $value, string $key): string
    {
        if (empty($value)) {
            return '';
        }

        $encrypted = Security::encrypt($value, $this->getEncryptionKey());
        $base64 = base64_encode($encrypted);

        return '{encrypted}' . $base64;
    }

    /**
     * Decrypt a setting value
     *
     * @param string|null $value Encrypted value
     * @param string $key Setting key (for context)
     * @return string Plain text value
     */
    protected function decryptSetting(?string $value, string $key): string
    {
        if (empty($value)) {
            return '';
        }

        if (!str_starts_with($value, '{encrypted}')) {
            return $value;
        }

        $base64Value = substr($value, 11);
        $encryptedValue = base64_decode($base64Value, true);

        if ($encryptedValue === false) {
            \Cake\Log\Log::error('Failed to base64 decode setting: ' . $key);
            return '';
        }

        try {
            $decrypted = Security::decrypt($encryptedValue, $this->getEncryptionKey());

            if ($decrypted === false || $decrypted === null) {
                return '';
            }

            return (string)$decrypted;
        } catch (\Exception $e) {
            \Cake\Log\Log::error('Failed to decrypt setting: ' . $key, [
                'error' => $e->getMessage()
            ]);
            return '';
        }
    }

    /**
     * Get encryption key from app configuration
     *
     * @return string Encryption key
     * @throws \RuntimeException If Security.salt is not configured
     */
    private function getEncryptionKey(): string
    {
        $salt = Security::getSalt();

        if (empty($salt)) {
            throw new \RuntimeException(
                'Security.salt is not configured. Please set SECURITY_SALT environment variable.'
            );
        }

        return $salt;
    }

    /**
     * Process settings array - decrypt encrypted values
     *
     * @param array $settings Array of setting_key => setting_value
     * @return array Processed settings with decrypted values
     */
    protected function processSettings(array $settings): array
    {
        $processed = [];

        foreach ($settings as $key => $value) {
            if ($this->shouldEncrypt($key)) {
                $processed[$key] = $this->decryptSetting($value, $key);
            } else {
                $processed[$key] = $value;
            }
        }

        return $processed;
    }
}
```

- [ ] **Step 2: Verificar sintaxis**

Run: `php -l src/Service/Traits/SettingsEncryptionTrait.php`
Expected: `No syntax errors detected`.

---

### Task 7: Regenerar autoload + commit Fase A

**Files:** todos los nuevos en `src/Constants/` y `src/Service/Traits/SettingsEncryptionTrait.php`.

- [ ] **Step 1: Regenerar autoload**

Run: `composer dump-autoload`
Expected: `Generating autoload files` + `Generated autoload files containing N classes`.

- [ ] **Step 2: Verificar la app sigue arrancando**

Run: `bin/cake routes 2>&1 | head -20`
Expected: lista de rutas (no errores fatales). Si hay error de namespace, los archivos nuevos no autoload-ean — revisar PSR-4.

- [ ] **Step 3: Smoke test browser**

Abrir manualmente `http://localhost:8765/` (o `http://localhost:8082/` si Docker está activo) y verificar que la home carga sin error. Login y entrar al detalle de un ticket para confirmar.

- [ ] **Step 4: Stage y commit Fase A**

```bash
git add src/Constants/ src/Service/Traits/SettingsEncryptionTrait.php
git status
```

Expected: 5 archivos nuevos staged, ningún archivo modificado.

```bash
git commit -m "$(cat <<'EOF'
feat(constants): create src/Constants/ with TicketConstants, RoleConstants, CacheConstants, SettingKeys

Phase A of Críticos 1-3 refactor: additive creation of new constants
classes. No consumers migrated yet; src/Utility/ untouched. Also moves
SettingsEncryptionTrait to src/Service/Traits/ alongside other service mixins.

Spec: docs/superpowers/specs/2026-05-07-criticos-1-3-design.md
EOF
)"
```

---

## Fase B — Migrar consumidores a `App\Constants\*`

> Cada task de esta fase agrupa archivos por capa. Tras cada task: ejecutar `composer cs-check` y un smoke test corto del flujo afectado, luego commit.

### Task 8: Migrar `AppController` y `WebhooksController`

**Files:**
- Modify: `src/Controller/AppController.php`
- Modify: `src/Controller/WebhooksController.php`

- [ ] **Step 1: Reemplazar imports en AppController**

Edit `src/Controller/AppController.php`:

OLD:
```php
use App\Utility\SettingKeys;
use App\Utility\SettingsEncryptionTrait;
use App\Utility\ValidationConstants;
```

NEW:
```php
use App\Constants\CacheConstants;
use App\Constants\RoleConstants;
use App\Constants\SettingKeys;
use App\Service\Traits\SettingsEncryptionTrait;
```

(Si el archivo solo tenía algunos de estos imports, ajustar a los que existían. El orden alfabético es la convención CakePHP.)

- [ ] **Step 2: Reemplazar usos de `ValidationConstants::` en AppController**

Edit `src/Controller/AppController.php`:

| Reemplazar | Por |
|---|---|
| `ValidationConstants::CACHE_SETTINGS` | `CacheConstants::CACHE_SETTINGS` |
| `ValidationConstants::CACHE_CONFIG` | `CacheConstants::CACHE_CONFIG` |
| `ValidationConstants::DEFAULT_SYSTEM_TITLE` | `CacheConstants::DEFAULT_SYSTEM_TITLE` |
| `ValidationConstants::ROLE_ADMIN` | `RoleConstants::ROLE_ADMIN` |
| `ValidationConstants::ROLE_AGENT` | `RoleConstants::ROLE_AGENT` |
| `ValidationConstants::ROLE_SERVICIO_CLIENTE` | `RoleConstants::ROLE_SERVICIO_CLIENTE` |
| `ValidationConstants::ROLE_REQUESTER` | `RoleConstants::ROLE_REQUESTER` |

Verificar después con: `grep -n "ValidationConstants" src/Controller/AppController.php`
Expected: cero líneas.

- [ ] **Step 3: Reemplazar import en WebhooksController**

Edit `src/Controller/WebhooksController.php`:

OLD: `use App\Utility\SettingKeys;`
NEW: `use App\Constants\SettingKeys;`

(Las referencias a `SettingKeys::*` no cambian de nombre.)

Verificar: `grep -n "App\\\\Utility" src/Controller/WebhooksController.php`
Expected: cero líneas.

- [ ] **Step 4: Smoke test**

- Cargar home `/` (autenticado): debe pasar el `beforeFilter` de AppController sin error.
- Probar `/admin/settings` para verificar `RoleConstants::ROLE_ADMIN` funciona.

- [ ] **Step 5: Commit**

```bash
git add src/Controller/AppController.php src/Controller/WebhooksController.php
git commit -m "refactor(controllers): migrate AppController and WebhooksController to App\\\\Constants"
```

---

### Task 9: Migrar `TicketsController` (solo imports + símbolos `RoleConstants`/`CacheConstants`)

> En esta task solo se migran imports y símbolos no relacionados con estados. Los literales de status/priority y métodos legacy (`getStatusConfig`, `getResolvedStatuses`, etc.) se atacan en Fase C.

**Files:**
- Modify: `src/Controller/TicketsController.php`

- [ ] **Step 1: Reemplazar imports**

Edit `src/Controller/TicketsController.php`:

Reemplazar:
```php
use App\Utility\ValidationConstants;
```

Por (alfabético):
```php
use App\Constants\CacheConstants;
use App\Constants\RoleConstants;
```

(Si el archivo importaba `SettingKeys` desde `App\Utility`, también moverlo a `App\Constants`.)

- [ ] **Step 2: Reemplazar símbolos**

| Reemplazar | Por |
|---|---|
| `ValidationConstants::ROLE_ADMIN` | `RoleConstants::ROLE_ADMIN` |
| `ValidationConstants::ROLE_AGENT` | `RoleConstants::ROLE_AGENT` |
| `ValidationConstants::ROLE_REQUESTER` | `RoleConstants::ROLE_REQUESTER` |
| `ValidationConstants::CACHE_SETTINGS` | `CacheConstants::CACHE_SETTINGS` |
| `ValidationConstants::CACHE_CONFIG` | `CacheConstants::CACHE_CONFIG` |

- [ ] **Step 3: Verificar grep**

Run: `grep -n "ValidationConstants\|App\\\\Utility" src/Controller/TicketsController.php`
Expected: cero líneas.

- [ ] **Step 4: Smoke test**

Cargar `/tickets` autenticado como admin y como agente; verificar que se renderiza sin error.

- [ ] **Step 5: Commit**

```bash
git add src/Controller/TicketsController.php
git commit -m "refactor(tickets): migrate TicketsController imports to App\\\\Constants"
```

---

### Task 10: Migrar Admin Controllers (`SettingsController`, `TagsController`, `EmailTemplatesController`)

**Files:**
- Modify: `src/Controller/Admin/SettingsController.php`
- Modify: `src/Controller/Admin/TagsController.php`
- Modify: `src/Controller/Admin/EmailTemplatesController.php`

- [ ] **Step 1: Migrar `SettingsController`**

Edit `src/Controller/Admin/SettingsController.php`:

Reemplazar imports:
```php
use App\Utility\SettingKeys;
use App\Utility\ValidationConstants;
```

Por:
```php
use App\Constants\RoleConstants;
use App\Constants\SettingKeys;
```

Reemplazar símbolos:
- `ValidationConstants::ROLE_ADMIN` → `RoleConstants::ROLE_ADMIN`
- `ValidationConstants::STAFF_ROLES` → `RoleConstants::STAFF_ROLES`

Verificar: `grep -n "ValidationConstants\|App\\\\Utility" src/Controller/Admin/SettingsController.php`
Expected: cero líneas.

- [ ] **Step 2: Migrar `TagsController`**

Edit `src/Controller/Admin/TagsController.php`:

Reemplazar import:
```php
use App\Utility\ValidationConstants;
```

Por:
```php
use App\Constants\RoleConstants;
```

Reemplazar símbolos:
- `ValidationConstants::ROLE_ADMIN` → `RoleConstants::ROLE_ADMIN`

- [ ] **Step 3: Migrar `EmailTemplatesController`**

Edit `src/Controller/Admin/EmailTemplatesController.php`:

Reemplazar import:
```php
use App\Utility\ValidationConstants;
```

Por:
```php
use App\Constants\CacheConstants;
use App\Constants\RoleConstants;
```

Reemplazar símbolos:
- `ValidationConstants::ROLE_ADMIN` → `RoleConstants::ROLE_ADMIN`
- `ValidationConstants::DEFAULT_SYSTEM_TITLE` → `CacheConstants::DEFAULT_SYSTEM_TITLE`

- [ ] **Step 4: Verificar grep en los 3 archivos**

Run: `grep -rn "ValidationConstants\|App\\\\Utility" src/Controller/Admin/`
Expected: cero líneas.

- [ ] **Step 5: Smoke test**

Cargar como admin: `/admin/settings`, `/admin/tags`, `/admin/email-templates`. Las tres deben renderizar sin error.

- [ ] **Step 6: Commit**

```bash
git add src/Controller/Admin/
git commit -m "refactor(admin): migrate Admin controllers to App\\\\Constants"
```

---

### Task 11: Migrar Tables (`TicketsTable`, `TicketCommentsTable`, `UsersTable`)

> Solo imports + símbolos. Los filtros de estado en `TicketsTable::findForView` se tocan en Fase C.

**Files:**
- Modify: `src/Model/Table/TicketsTable.php`
- Modify: `src/Model/Table/TicketCommentsTable.php`
- Modify: `src/Model/Table/UsersTable.php`

- [ ] **Step 1: Migrar `TicketsTable`**

Edit `src/Model/Table/TicketsTable.php`:

Reemplazar import:
```php
use App\Utility\ValidationConstants;
```

Por:
```php
use App\Constants\TicketConstants;
```

Reemplazar símbolos en validators:
- `ValidationConstants::TICKET_STATUSES` → `TicketConstants::STATUSES`
- `ValidationConstants::PRIORITIES` → `TicketConstants::PRIORITIES`

(Si hay otros usos como `ValidationConstants::STATUS_NUEVO` etc, → `TicketConstants::STATUS_NUEVO`.)

- [ ] **Step 2: Migrar `TicketCommentsTable`**

Edit `src/Model/Table/TicketCommentsTable.php`:

Reemplazar import:
```php
use App\Utility\ValidationConstants;
```

Por:
```php
use App\Constants\TicketConstants;
```

Reemplazar símbolo:
- `ValidationConstants::TICKET_COMMENT_TYPES` → `TicketConstants::ALL_COMMENT_TYPES`

(`ALL_COMMENT_TYPES` incluye `system` igual que `TICKET_COMMENT_TYPES` original — preserva semántica del validator.)

- [ ] **Step 3: Migrar `UsersTable`**

Edit `src/Model/Table/UsersTable.php`:

Reemplazar import:
```php
use App\Utility\ValidationConstants;
```

Por:
```php
use App\Constants\RoleConstants;
```

Reemplazar símbolo:
- `ValidationConstants::ROLES` → `RoleConstants::ROLES`

- [ ] **Step 4: Verificar grep en los 3 archivos**

Run: `grep -rn "ValidationConstants\|App\\\\Utility" src/Model/Table/`
Expected: cero líneas.

- [ ] **Step 5: Smoke test**

- Crear ticket nuevo desde `/tickets/add` con priority='media', status='nuevo'. Debe guardar.
- Crear comentario público desde detalle de ticket. Debe guardar.
- Listar usuarios desde `/admin/settings` (sección users). Debe cargar sin error.

- [ ] **Step 6: Commit**

```bash
git add src/Model/Table/TicketsTable.php src/Model/Table/TicketCommentsTable.php src/Model/Table/UsersTable.php
git commit -m "refactor(tables): migrate validators to App\\\\Constants\\\\TicketConstants and RoleConstants"
```

---

### Task 12: Migrar Services grupo 1 (`EmailService`, `EmailTemplateRenderer`, `N8nService`)

**Files:**
- Modify: `src/Service/EmailService.php`
- Modify: `src/Service/EmailTemplateRenderer.php`
- Modify: `src/Service/N8nService.php`

- [ ] **Step 1: Migrar `EmailService`**

Edit `src/Service/EmailService.php`:

Reemplazar import:
```php
use App\Utility\SettingKeys;
use App\Utility\ValidationConstants;
```

Por:
```php
use App\Constants\CacheConstants;
use App\Constants\SettingKeys;
```

Reemplazar:
- `ValidationConstants::DEFAULT_SYSTEM_TITLE` → `CacheConstants::DEFAULT_SYSTEM_TITLE`

- [ ] **Step 2: Migrar `EmailTemplateRenderer`**

Edit `src/Service/EmailTemplateRenderer.php`:

Mismo patrón que EmailService.

- [ ] **Step 3: Migrar `N8nService`**

Edit `src/Service/N8nService.php`:

Reemplazar import:
```php
use App\Utility\SettingKeys;
```

Por:
```php
use App\Constants\SettingKeys;
```

(N8nService solo usa `SettingKeys::*`, no `ValidationConstants`.)

- [ ] **Step 4: Verificar grep**

Run: `grep -rn "App\\\\Utility" src/Service/EmailService.php src/Service/EmailTemplateRenderer.php src/Service/N8nService.php`
Expected: cero líneas.

- [ ] **Step 5: Smoke test**

- Trigger una notificación: cambiar estado de un ticket → debe enviar email/n8n webhook sin error en `logs/error.log`.

- [ ] **Step 6: Commit**

```bash
git add src/Service/EmailService.php src/Service/EmailTemplateRenderer.php src/Service/N8nService.php
git commit -m "refactor(services): migrate Email/N8n services to App\\\\Constants"
```

---

### Task 13: Migrar Services grupo 2 (`WhatsappService`, `AuthorizationService`, `SettingsService`, `SidebarCountsService`)

**Files:**
- Modify: `src/Service/WhatsappService.php`
- Modify: `src/Service/AuthorizationService.php`
- Modify: `src/Service/SettingsService.php`
- Modify: `src/Service/SidebarCountsService.php`

- [ ] **Step 1: Migrar `WhatsappService`**

Edit `src/Service/WhatsappService.php`:

Reemplazar import: `use App\Utility\SettingKeys;` → `use App\Constants\SettingKeys;`.

- [ ] **Step 2: Migrar `AuthorizationService`**

Edit `src/Service/AuthorizationService.php`:

Reemplazar import: `use App\Utility\ValidationConstants;` → `use App\Constants\RoleConstants;`.

Reemplazar símbolos: `ValidationConstants::ROLE_*` → `RoleConstants::ROLE_*` (admin, agent, requester, servicio_cliente — los que aparezcan).

- [ ] **Step 3: Migrar `SettingsService`**

Edit `src/Service/SettingsService.php`:

Reemplazar imports:
```php
use App\Utility\SettingKeys;
use App\Utility\ValidationConstants;
```

Por (los que aparezcan):
```php
use App\Constants\CacheConstants;
use App\Constants\SettingKeys;
```

Reemplazar:
- `ValidationConstants::CACHE_SETTINGS` → `CacheConstants::CACHE_SETTINGS`
- `ValidationConstants::CACHE_CONFIG` → `CacheConstants::CACHE_CONFIG`

- [ ] **Step 4: Migrar `SidebarCountsService` (solo imports en este task; el array literal `['resuelto','convertido']` se trata en Fase C)**

Edit `src/Service/SidebarCountsService.php`:

Reemplazar import: `use App\Utility\ValidationConstants;` → `use App\Constants\TicketConstants;`.

Si tiene referencias a `ValidationConstants::STATUS_*` o similares, reemplazar por `TicketConstants::*`.

- [ ] **Step 5: Verificar grep**

Run: `grep -rn "App\\\\Utility" src/Service/WhatsappService.php src/Service/AuthorizationService.php src/Service/SettingsService.php src/Service/SidebarCountsService.php`
Expected: cero líneas.

- [ ] **Step 6: Smoke test**

- Configurar Whatsapp en `/admin/settings`, guardar — verificar que `SettingsService` y `WhatsappService` no fallan.
- Cargar `/tickets`: el sidebar (que usa `SidebarCountsService`) renderiza contadores.

- [ ] **Step 7: Commit**

```bash
git add src/Service/WhatsappService.php src/Service/AuthorizationService.php src/Service/SettingsService.php src/Service/SidebarCountsService.php
git commit -m "refactor(services): migrate Whatsapp/Auth/Settings/SidebarCounts to App\\\\Constants"
```

---

### Task 14: Migrar `GmailImportService`, `GmailService`, `ConfigResolutionTrait`

**Files:**
- Modify: `src/Service/GmailImportService.php`
- Modify: `src/Service/GmailService.php`
- Modify: `src/Service/Traits/ConfigResolutionTrait.php`

- [ ] **Step 1: Migrar `GmailImportService`**

Edit `src/Service/GmailImportService.php`:

Reemplazar imports:
```php
use App\Utility\SettingKeys;
use App\Utility\SettingsEncryptionTrait;
```

Por:
```php
use App\Constants\SettingKeys;
use App\Service\Traits\SettingsEncryptionTrait;
```

(El uso del trait `use SettingsEncryptionTrait;` dentro de la clase no cambia.)

- [ ] **Step 2: Migrar `GmailService`**

Edit `src/Service/GmailService.php`:

Reemplazar import: `use App\Utility\SettingKeys;` → `use App\Constants\SettingKeys;`.

- [ ] **Step 3: Migrar `ConfigResolutionTrait`**

Edit `src/Service/Traits/ConfigResolutionTrait.php`:

Reemplazar imports `App\Utility\*` → equivalentes en `App\Constants\*`.

- [ ] **Step 4: Verificar grep**

Run: `grep -rn "App\\\\Utility" src/Service/GmailImportService.php src/Service/GmailService.php src/Service/Traits/ConfigResolutionTrait.php`
Expected: cero líneas.

- [ ] **Step 5: Smoke test (importante: Gmail)**

- Si Gmail está configurado: ejecutar `bin/cake import_gmail --max 1` y verificar que no falla con error de namespace ni de trait.
- Si no está configurado: cargar `/admin/settings/gmail` y verificar que la página renderiza.

- [ ] **Step 6: Commit**

```bash
git add src/Service/GmailImportService.php src/Service/GmailService.php src/Service/Traits/ConfigResolutionTrait.php
git commit -m "refactor(services): migrate Gmail services and ConfigResolutionTrait to App\\\\Constants"
```

---

### Task 15: Migrar Views (`TicketsSidebarCell`, template `Admin/Settings/index.php`)

**Files:**
- Modify: `src/View/Cell/TicketsSidebarCell.php`
- Modify: `templates/Admin/Settings/index.php`

- [ ] **Step 1: Migrar `TicketsSidebarCell` (solo imports — el array literal se trata en Fase C)**

Edit `src/View/Cell/TicketsSidebarCell.php`:

Reemplazar import: `use App\Utility\ValidationConstants;` → `use App\Constants\TicketConstants;`.

Si hay usos de `ValidationConstants::STATUS_*`, reemplazar.

- [ ] **Step 2: Migrar template `Admin/Settings/index.php`**

Edit `templates/Admin/Settings/index.php`:

Reemplazar cualquier `App\Utility\SettingKeys` o `\App\Utility\ValidationConstants` por sus equivalentes en `App\Constants`.

(En templates, las referencias suelen aparecer como `\App\Utility\SettingKeys::FOO` con leading backslash. Cambiar a `\App\Constants\SettingKeys::FOO`.)

- [ ] **Step 3: Verificar grep**

Run: `grep -rn "App\\\\Utility" src/View/ templates/`
Expected: cero líneas.

- [ ] **Step 4: Smoke test**

- Cargar `/admin/settings` para confirmar el template renderiza.
- Cargar `/tickets` para confirmar el sidebar Cell renderiza.

- [ ] **Step 5: Commit**

```bash
git add src/View/Cell/TicketsSidebarCell.php templates/Admin/Settings/index.php
git commit -m "refactor(views): migrate TicketsSidebarCell and Admin/Settings template to App\\\\Constants"
```

---

### Task 16: Verificación final Fase B

**Files:** ninguno (solo verificación).

- [ ] **Step 1: Cero referencias restantes a `App\Utility\` o `ValidationConstants`**

Run: `grep -rn "App\\\\Utility\\\\\(SettingKeys\|SettingsEncryptionTrait\|ValidationConstants\)\|ValidationConstants::" src/ config/ templates/ webroot/ 2>&1 | grep -v "src/Utility/"`
Expected: cero líneas (excluimos `src/Utility/` porque los archivos viejos siguen ahí, sin consumidores).

- [ ] **Step 2: Coding standard**

Run: `composer cs-check`
Expected: `... no errors`.

Si hay errores: `composer cs-fix && git add -u && git commit -m "style: cs-fix after Phase B"`.

- [ ] **Step 3: Smoke test integrado**

Manualmente probar:
- Login.
- Home `/`.
- `/tickets` listado.
- Detalle de un ticket cualquiera.
- Crear comentario público.
- `/admin/settings` carga.

Sin errores en `logs/error.log`.

- [ ] **Step 4: Tag intermedio**

```bash
git tag fase-b-completa
```

---

## Fase B.5 — Migración de datos legacy

### Task 17: Backup de BD pre-migration

**Files:** ninguno (operación de BD).

- [ ] **Step 1: Identificar credenciales de BD**

Run: `grep -E "DB_(HOST|DATABASE|USERNAME|PASSWORD)" config/app_local.php docker-compose.yml 2>&1 | head -10`

Tomar nota de host, database, username (no del password — usar variable de entorno o prompt interactivo en el `mysqldump`).

- [ ] **Step 2: Conteo previo de tickets por estado**

Run (ajustar host/user/db según tu entorno):
```bash
mysql -h <DB_HOST> -u <DB_USERNAME> -p <DB_DATABASE> -e "SELECT status, COUNT(*) AS cnt FROM tickets GROUP BY status ORDER BY status;"
```

Expected output (algo así, números varían):
```
+--------------+-----+
| status       | cnt |
+--------------+-----+
| abierto      |  XX |
| convertido   |   X |   ← serán migrados
| nuevo        |  XX |
| pendiente    |  XX |
| resuelto     |  XX |
+--------------+-----+
```

Anotar el conteo de `convertido`, `cerrado`, `en_progreso` (los que aparezcan) — debe coincidir con la verificación post-migration.

- [ ] **Step 3: Hacer mysqldump del schema completo**

Run:
```bash
mysqldump -h <DB_HOST> -u <DB_USERNAME> -p <DB_DATABASE> tickets > /tmp/tickets-pre-criticos-1-3-$(date +%Y%m%d-%H%M%S).sql
```

Expected: archivo `.sql` creado en `/tmp/`. Verificar tamaño:
```bash
ls -lh /tmp/tickets-pre-criticos-1-3-*.sql
```

Debe ser > 0 bytes. Guardar la ruta exacta para rollback de emergencia.

---

### Task 18: Crear migration `ConsolidateLegacyTicketStatuses`

**Files:**
- Create: `config/Migrations/<timestamp>_ConsolidateLegacyTicketStatuses.php` (timestamp lo genera CakePHP)

- [ ] **Step 1: Bake migration**

Run: `bin/cake bake migration ConsolidateLegacyTicketStatuses`
Expected: imprime `Creating file config/Migrations/<YYYYMMDDhhmmss>_ConsolidateLegacyTicketStatuses.php` y crea el esqueleto.

- [ ] **Step 2: Reemplazar contenido del archivo generado**

Edit el archivo `config/Migrations/<timestamp>_ConsolidateLegacyTicketStatuses.php` y reemplazar todo su contenido por:

```php
<?php
declare(strict_types=1);

use Migrations\AbstractMigration;

class ConsolidateLegacyTicketStatuses extends AbstractMigration
{
    /**
     * Consolida tickets con estados legacy (convertido/cerrado/en_progreso)
     * en `resuelto`. Estados eliminados como parte de Críticos 1-3.
     *
     * Spec: docs/superpowers/specs/2026-05-07-criticos-1-3-design.md §3 Fase B.5
     */
    public function up(): void
    {
        $this->execute("
            UPDATE tickets
            SET status = 'resuelto',
                resolved_at = COALESCE(resolved_at, updated)
            WHERE status IN ('convertido', 'cerrado', 'en_progreso')
        ");
    }

    public function down(): void
    {
        // No reversible: el UPDATE pierde la distinción entre convertido/cerrado/en_progreso.
        // Si es necesario revertir, restaurar desde backup de BD pre-migration
        // (ver Task 17 del plan de implementación).
        throw new \RuntimeException(
            'ConsolidateLegacyTicketStatuses is not reversible. Restore from backup.'
        );
    }
}
```

- [ ] **Step 3: Verificar sintaxis**

Run: `php -l config/Migrations/*ConsolidateLegacyTicketStatuses.php`
Expected: `No syntax errors detected`.

- [ ] **Step 4: Verificar que la migration está pendiente**

Run: `bin/cake migrations status`
Expected: la nueva migration aparece con estado `down` (pendiente).

---

### Task 19: Aplicar migration y verificar BD

**Files:** ninguno (operación de BD).

- [ ] **Step 1: Confirmar backup existe**

Run: `ls -lh /tmp/tickets-pre-criticos-1-3-*.sql`
Expected: el archivo del Task 17 sigue ahí. Si NO existe, regresar al Task 17 y rehacer.

- [ ] **Step 2: Aplicar migrations**

Run: `bin/cake migrations migrate`
Expected: la migration `ConsolidateLegacyTicketStatuses` aparece con `Migrating` y luego `Migrated`. Sin errores.

- [ ] **Step 3: Verificar conteo post-migration**

Run:
```bash
mysql -h <DB_HOST> -u <DB_USERNAME> -p <DB_DATABASE> -e "SELECT status, COUNT(*) AS cnt FROM tickets GROUP BY status ORDER BY status;"
```

Expected: solo aparecen `nuevo`, `abierto`, `pendiente`, `resuelto`. **Cero filas** con `convertido`/`cerrado`/`en_progreso`.

El conteo total de `resuelto` debe haber crecido en exactamente la suma de los conteos legacy del Task 17 Step 2.

- [ ] **Step 4: Verificar `resolved_at` poblado**

Run:
```bash
mysql -h <DB_HOST> -u <DB_USERNAME> -p <DB_DATABASE> -e "SELECT COUNT(*) AS sin_resolved_at FROM tickets WHERE status='resuelto' AND resolved_at IS NULL;"
```

Expected: idealmente `0`. Si hay alguno, son tickets donde tanto `resolved_at` como `updated` eran NULL — caso patológico, dejar que se resuelva manualmente luego.

- [ ] **Step 5: Smoke test**

- Cargar `/tickets?view=resueltos`: debe listar todos los tickets ahora resueltos (incluyendo los migrados).
- Cargar `/tickets?view=convertidos` (URL directa): no debe existir el view; cae al listado por defecto. **No** debe romper la página.

- [ ] **Step 6: Commit la migration**

```bash
git add config/Migrations/*ConsolidateLegacyTicketStatuses.php
git commit -m "$(cat <<'EOF'
feat(db): consolidate legacy ticket statuses (convertido/cerrado/en_progreso) into resuelto

Migration irreversible. Backup tomado en /tmp/tickets-pre-criticos-1-3-*.sql
antes de aplicar. Spec: docs/superpowers/specs/2026-05-07-criticos-1-3-design.md
EOF
)"
```

---

## Fase C — Eliminar referencias legacy y mapas duplicados

### Task 20: Refactor `StatusHelper` — leer de `TicketConstants`

**Files:**
- Modify: `src/View/Helper/StatusHelper.php`

- [ ] **Step 1: Reemplazar contenido completo del archivo**

Edit `src/View/Helper/StatusHelper.php` y reemplazar todo el contenido por:

```php
<?php
declare(strict_types=1);

namespace App\View\Helper;

use App\Constants\TicketConstants;
use Cake\View\Helper;

/**
 * Status Helper
 *
 * Leitura delgada sobre TicketConstants. Genera badges HTML con estilos
 * inline (deprecated; reemplazar por elementos en próximo ciclo — ver
 * Alto 4.4 de la auditoría).
 */
class StatusHelper extends Helper
{
    public function priorityColor(string $priority): string
    {
        return TicketConstants::PRIORITY_COLORS[strtolower($priority)] ?? '#6c757d';
    }

    public function priorityLabel(string $priority): string
    {
        return TicketConstants::PRIORITY_LABELS[strtolower($priority)] ?? ucfirst($priority);
    }

    public function statusColor(string $status): string
    {
        return TicketConstants::STATUS_COLORS[strtolower($status)] ?? '#6c757d';
    }

    public function statusLabel(string $status): string
    {
        return TicketConstants::STATUS_LABELS[strtolower($status)]
            ?? ucfirst(str_replace('_', ' ', $status));
    }

    public function statusBadge(string $status, array $options = []): string
    {
        $badge = sprintf(
            '<span class="badge" style="background-color: %s; color: white; border-radius: 8px; padding: 0.35rem 0.65rem; font-size: 0.75rem; font-weight: 600; text-transform: uppercase;">%s</span>',
            h($this->statusColor($status)),
            h($this->statusLabel($status))
        );

        $url = $options['url'] ?? null;
        if ($url) {
            return $this->getView()->Html->link($badge, $url, ['escape' => false, 'class' => 'text-decoration-none']);
        }

        return $badge;
    }

    public function priorityBadge(string $priority, array $options = []): string
    {
        $badge = sprintf(
            '<span class="badge" style="background-color: %s; color: white; border-radius: 8px; padding: 0.35rem 0.65rem; font-size: 0.75rem; font-weight: 600; text-transform: uppercase;">%s</span>',
            h($this->priorityColor($priority)),
            h($this->priorityLabel($priority))
        );

        $url = $options['url'] ?? null;
        if ($url) {
            return $this->getView()->Html->link($badge, $url, ['escape' => false, 'class' => 'text-decoration-none']);
        }

        return $badge;
    }
}
```

- [ ] **Step 2: Verificar sintaxis**

Run: `php -l src/View/Helper/StatusHelper.php`
Expected: `No syntax errors detected`.

- [ ] **Step 3: Smoke test visual**

Cargar `/tickets`. Cada badge de estado y prioridad debe renderizarse con su color correcto. Comparar visualmente con antes del refactor — los colores deben ser idénticos.

- [ ] **Step 4: Commit**

```bash
git add src/View/Helper/StatusHelper.php
git commit -m "refactor(view): rewrite StatusHelper as thin layer over TicketConstants"
```

---

### Task 21: Refactor métodos de status en `TicketsController`

**Files:**
- Modify: `src/Controller/TicketsController.php`

- [ ] **Step 1: Asegurar import de `TicketConstants`**

Edit `src/Controller/TicketsController.php`:

Si no está ya, agregar (alfabético):
```php
use App\Constants\TicketConstants;
```

- [ ] **Step 2: Reemplazar `getStatusConfig()`**

Find (líneas ~334-343 — el bloque puede haberse movido por commits anteriores; localizar por el nombre del método):

```php
    protected function getStatusConfig(): array
    {
        return [
            'nuevo' => ['icon' => 'bi-circle-fill', 'color' => '#ffc107', 'label' => 'Nuevo'],
            'abierto' => ['icon' => 'bi-circle-fill', 'color' => '#dc3545', 'label' => 'Abierto'],
            'pendiente' => ['icon' => 'bi-circle-fill', 'color' => '#0d6efd', 'label' => 'Pendiente'],
            'resuelto' => ['icon' => 'bi-circle-fill', 'color' => '#198754', 'label' => 'Resuelto'],
            'convertido' => ['icon' => 'bi-arrow-left-right', 'color' => '#6c757d', 'label' => 'Convertido'],
        ];
    }
```

Replace with:

```php
    protected function getStatusConfig(): array
    {
        $config = [];
        foreach (TicketConstants::STATUSES as $status) {
            $config[$status] = [
                'icon'  => TicketConstants::STATUS_ICONS[$status] ?? 'bi-circle-fill',
                'color' => TicketConstants::STATUS_COLORS[$status] ?? '#6c757d',
                'label' => TicketConstants::STATUS_LABELS[$status] ?? ucfirst($status),
            ];
        }
        return $config;
    }
```

- [ ] **Step 3: Reemplazar `getResolvedStatuses()`**

Find:
```php
    protected function getResolvedStatuses(): array
    {
        return ['resuelto', 'convertido'];
    }
```

Replace with:
```php
    protected function getResolvedStatuses(): array
    {
        return TicketConstants::RESOLVED_STATUSES;
    }
```

- [ ] **Step 4: Quitar `cerrado` de `getStatusesForEntity` (línea ~606)**

Find (en el método que retorna labels para el entity 'ticket'):
```php
                'pendiente' => 'Pendiente',
                'resuelto' => 'Resuelto',
                'cerrado' => 'Cerrado',
```

Replace with (quitar la entrada `cerrado`):
```php
                'pendiente' => 'Pendiente',
                'resuelto' => 'Resuelto',
```

- [ ] **Step 5: Quitar el filtro `'convertido'` (línea ~646)**

Find una expresión similar a:
```php
        $statuses = array_filter($allStatuses, function ($key) {
            return $key !== 'convertido';
        });
```

Replace with: eliminar el `array_filter` completo y usar `$allStatuses` directamente. O equivalentemente:

```php
        $statuses = $allStatuses;
```

(El uso aguas abajo no necesita cambios — el array ya está libre de `convertido`.)

- [ ] **Step 6: Verificar grep**

Run: `grep -n "convertido\|cerrado\|en_progreso" src/Controller/TicketsController.php`
Expected: cero líneas.

- [ ] **Step 7: Smoke test**

- Cargar `/tickets`: dropdown de filtro de estado muestra solo Nuevo/Abierto/Pendiente/Resuelto.
- Cambiar estado de un ticket a `resuelto` desde el detalle: `isEntityLocked` debe retornar `true` (controles de edición se bloquean).

- [ ] **Step 8: Commit**

```bash
git add src/Controller/TicketsController.php
git commit -m "refactor(tickets): replace status literals with TicketConstants in TicketsController"
```

---

### Task 22: Refactor `SidebarCountsService`

**Files:**
- Modify: `src/Service/SidebarCountsService.php`

- [ ] **Step 1: Reemplazar literal `['resuelto', 'convertido']`**

Edit `src/Service/SidebarCountsService.php`:

Find (línea ~28):
```php
        $resolvedStatuses = ['resuelto', 'convertido'];
```

Replace with:
```php
        $resolvedStatuses = TicketConstants::RESOLVED_STATUSES;
```

(`TicketConstants` ya está importado por Task 13.)

- [ ] **Step 2: Verificar grep**

Run: `grep -n "convertido\|cerrado\|en_progreso" src/Service/SidebarCountsService.php`
Expected: cero líneas.

- [ ] **Step 3: Smoke test**

Cargar `/tickets` autenticado. Los contadores del sidebar deben mostrar valores razonables (no negativos, no `null`).

- [ ] **Step 4: Commit**

```bash
git add src/Service/SidebarCountsService.php
git commit -m "refactor(sidebar): use TicketConstants::RESOLVED_STATUSES in SidebarCountsService"
```

---

### Task 23: Refactor `TicketService` — eliminar rama `cerrado`

**Files:**
- Modify: `src/Service/TicketService.php`

- [ ] **Step 1: Eliminar bloque de `cerrado`**

Edit `src/Service/TicketService.php`:

Find (alrededor línea 633):
```php
        if ($newStatus === 'cerrado' && isset($entity->closed_at) && !$entity->closed_at) {
            // ... contenido del bloque ...
        }
```

Eliminar el bloque completo (incluyendo el `if` y su cuerpo). El resto de transiciones (`resuelto → resolved_at`) se mantiene intacto.

- [ ] **Step 2: Verificar grep**

Run: `grep -n "cerrado\|closed_at" src/Service/TicketService.php`
Expected: cero líneas.

- [ ] **Step 3: Smoke test**

- Cambiar estado de un ticket a `resuelto`: `resolved_at` se debe poblar.
- Cambiar estado de un ticket a `pendiente`: no debe romper.

- [ ] **Step 4: Commit**

```bash
git add src/Service/TicketService.php
git commit -m "refactor(tickets): remove dead 'cerrado' status branch from TicketService"
```

---

### Task 24: Refactor `NotificationRenderer`

**Files:**
- Modify: `src/Service/Renderer/NotificationRenderer.php`

- [ ] **Step 1: Asegurar import**

Edit `src/Service/Renderer/NotificationRenderer.php`:

Si no está, agregar:
```php
use App\Constants\TicketConstants;
```

- [ ] **Step 2: Reemplazar array de labels**

Find el bloque alrededor de líneas 58-62 (algo así):
```php
        $labels = [
            'nuevo' => 'Nuevo',
            'abierto' => 'Abierto',
            'pendiente' => 'Pendiente',
            'resuelto' => 'Resuelto',
            'cerrado' => 'Cerrado',
        ];
```

Replace with:
```php
        $labels = TicketConstants::STATUS_LABELS;
```

- [ ] **Step 3: Verificar grep**

Run: `grep -n "convertido\|cerrado\|en_progreso" src/Service/Renderer/NotificationRenderer.php`
Expected: cero líneas.

- [ ] **Step 4: Smoke test**

Cambiar estado de un ticket → debe enviar notificación email/n8n con el label correcto del status.

- [ ] **Step 5: Commit**

```bash
git add src/Service/Renderer/NotificationRenderer.php
git commit -m "refactor(notifications): use TicketConstants::STATUS_LABELS in NotificationRenderer"
```

---

### Task 25: Refactor filtros legacy en `TicketsTable::findForView`

**Files:**
- Modify: `src/Model/Table/TicketsTable.php`

- [ ] **Step 1: Asegurar import**

Si no está: `use App\Constants\TicketConstants;` (debe haber sido agregado en Task 11).

- [ ] **Step 2: Reemplazar `'Tickets.status NOT IN' => ['resuelto', 'convertido']`**

Edit `src/Model/Table/TicketsTable.php`:

Find líneas 227, 234, 247 (tres ocurrencias del mismo patrón):
```php
                        'Tickets.status NOT IN' => ['resuelto', 'convertido']
```

Replace cada una con:
```php
                        'Tickets.status NOT IN' => TicketConstants::RESOLVED_STATUSES
```

- [ ] **Step 3: Eliminar las cláusulas `'Tickets.status !=' => 'convertido'`**

Find (líneas 242, 282, 303 — tres ocurrencias):
```php
                            'Tickets.status !=' => 'convertido'
```

**Acciones:**
- Línea ~242 (case `creados_por_mi`): eliminar la línea (ya no es necesaria post-migration; los tickets `convertido` no existen). Cuidar la coma del array — el `where()` quedará con un solo elemento `'Tickets.requester_id'`.

  Antes:
  ```php
                  case 'creados_por_mi':
                      if ($user) {
                          $query->where([
                              'Tickets.requester_id' => $user->get('id'),
                              'Tickets.status !=' => 'convertido'
                          ]);
                      }
                      break;
  ```

  Después:
  ```php
                  case 'creados_por_mi':
                      if ($user) {
                          $query->where([
                              'Tickets.requester_id' => $user->get('id'),
                          ]);
                      }
                      break;
  ```

- Línea ~282 (case `recientes`): eliminar la línea.

  Antes:
  ```php
                  case 'recientes':
                      $query->where([
                          'Tickets.created >=' => date('Y-m-d', strtotime('-7 days')),
                          'Tickets.status !=' => 'convertido'
                      ]);
                      break;
  ```

  Después:
  ```php
                  case 'recientes':
                      $query->where([
                          'Tickets.created >=' => date('Y-m-d', strtotime('-7 days')),
                      ]);
                      break;
  ```

- Línea ~301-304 (bloque dentro del search): eliminar el `if ($view !== 'convertidos')` completo.

  Antes:
  ```php
              // Exclude converted tickets from search unless explicitly viewing convertidos
              if ($view !== 'convertidos') {
                  $query->where(['Tickets.status !=' => 'convertido']);
              }
  ```

  Después: borrar el bloque entero (4 líneas + comentario).

- [ ] **Step 4: Eliminar `case 'convertidos'`**

Find (líneas 276-278):
```php
                case 'convertidos':
                    $query->where(['Tickets.status' => 'convertido']);
                    break;
```

Replace with: eliminar el case completo (las 3 líneas).

- [ ] **Step 5: Verificar grep**

Run: `grep -n "convertido\|cerrado\|en_progreso" src/Model/Table/TicketsTable.php`
Expected: cero líneas.

- [ ] **Step 6: Smoke test**

- Cargar `/tickets?view=mis_tickets`: debe filtrar correctamente, mostrar solo asignados al usuario, excluyendo resueltos.
- Cargar `/tickets?view=resueltos`: debe mostrar solo resueltos.
- Cargar `/tickets?view=convertidos`: debe caer al listado por defecto silenciosamente, sin error.
- Cargar `/tickets?view=recientes`: debe mostrar tickets de últimos 7 días.
- Buscar `/tickets?search=foo`: debe ejecutar search sin error.

- [ ] **Step 7: Commit**

```bash
git add src/Model/Table/TicketsTable.php
git commit -m "refactor(tickets): remove convertido filter clauses from TicketsTable::findForView"
```

---

### Task 26: Refactor `TicketsSidebarCell`

**Files:**
- Modify: `src/View/Cell/TicketsSidebarCell.php`

- [ ] **Step 1: Reemplazar literal de open statuses**

Edit `src/View/Cell/TicketsSidebarCell.php`:

Find (línea ~42):
```php
                ->where(['assignee_id' => $userId, 'status IN' => ['nuevo', 'abierto', 'pendiente']])
```

Replace with:
```php
                ->where(['assignee_id' => $userId, 'status IN' => TicketConstants::OPEN_STATUSES])
```

(`TicketConstants` ya está importado en Task 15.)

- [ ] **Step 2: Eliminar línea de `convertidos`**

Find (línea ~56):
```php
            'convertidos' => $statusCounts['convertido'] ?? 0,
```

Replace with: eliminar la línea entera (cuidar la coma del array previo).

- [ ] **Step 3: Verificar grep**

Run: `grep -n "convertido\|cerrado\|en_progreso" src/View/Cell/TicketsSidebarCell.php`
Expected: cero líneas.

- [ ] **Step 4: Smoke test**

Cargar `/tickets`: el sidebar muestra los contadores; **no** debe aparecer un contador "Convertidos".

- [ ] **Step 5: Commit**

```bash
git add src/View/Cell/TicketsSidebarCell.php
git commit -m "refactor(view): use TicketConstants::OPEN_STATUSES in TicketsSidebarCell"
```

---

### Task 27: Refactor templates legacy

**Files:**
- Modify: `templates/Tickets/index.php`
- Modify: `templates/cell/TicketsSidebar/display.php`
- Modify: `templates/element/tickets/left_sidebar.php`

- [ ] **Step 1: Eliminar entrada `convertidos` en `templates/Tickets/index.php`**

Edit `templates/Tickets/index.php`:

Find (línea 41):
```php
                    'convertidos' => 'Tickets convertidos',
```

Replace with: eliminar la línea entera (cuidar la coma del array).

- [ ] **Step 2: Reemplazar `in_array(...)` con `TicketConstants` en `templates/Tickets/index.php`**

Find (línea 122):
```php
                                    $isLocked = in_array($ticket->status, ['resuelto', 'convertido']);
```

Replace with:
```php
                                    $isLocked = in_array($ticket->status, \App\Constants\TicketConstants::RESOLVED_STATUSES, true);
```

- [ ] **Step 3: Eliminar bloque "Convertidos" en `templates/cell/TicketsSidebar/display.php`**

Find (líneas 82-86, bloque del link "Convertidos"):
```php
                '<span class="badge" style="background-color: #6c757d; color: white;">⇄</span>Convertidos <span class="count">' . $counts['convertidos'] . '</span>',
                ['action' => 'index', '?' => ['view' => 'convertidos']],
                ['class' => $view === 'convertidos' ? 'active' : '', 'escape' => false]
```

Y la llamada `$this->Html->link(...)` que envuelve esto. Eliminar las ~5-7 líneas correspondientes al ítem "Convertidos" del sidebar (incluyendo `<li>...</li>` si aplica, según la estructura del template).

**Tip:** localizar visualmente el bloque buscando `'Convertidos'` en el archivo.

- [ ] **Step 4: Reemplazar `in_array(...)` en `templates/element/tickets/left_sidebar.php`**

Find (línea 7):
```php
        $isLocked = $isLocked ?? in_array($ticket->status, ['resuelto', 'convertido']);
```

Replace with:
```php
        $isLocked = $isLocked ?? in_array($ticket->status, \App\Constants\TicketConstants::RESOLVED_STATUSES, true);
```

- [ ] **Step 5: Verificar grep en templates**

Run: `grep -rn "convertido\|cerrado\|en_progreso" templates/`
Expected: cero líneas.

- [ ] **Step 6: Smoke test**

- Cargar `/tickets` en navegador: el sidebar **NO** muestra "Convertidos".
- Detalle de un ticket resuelto: badge gris-verde, controles bloqueados (`isLocked` true).
- Detalle de un ticket abierto: controles habilitados.

- [ ] **Step 7: Commit**

```bash
git add templates/
git commit -m "refactor(templates): remove convertidos sidebar tab and use TicketConstants in views"
```

---

### Task 28: Verificación Fase C

**Files:** ninguno (solo verificación).

- [ ] **Step 1: Cero literales legacy en código**

Run:
```bash
grep -rn -E "(en_progreso|convertido|cerrado)" src/ templates/ 2>&1 | grep -v 'cerrado sesión'
```

Expected: **cero** líneas. Si retorna alguna, identificar el archivo y limpiar antes de seguir.

- [ ] **Step 2: Coding standard**

Run: `composer cs-check`
Expected: `... no errors`.

Si hay errores: `composer cs-fix && git add -u && git commit -m "style: cs-fix after Phase C"`.

- [ ] **Step 3: Smoke test exhaustivo**

- Login con admin: verifica `/admin/settings`, `/admin/tags`, `/admin/email-templates`.
- Login con agent: verifica `/tickets?view=mis_tickets`, crear comentario, cambiar estado a `resuelto`, ver el badge cambiar.
- Login con requester: verifica `/tickets?view=creados_por_mi`.
- Cambiar estado de un ticket en cada dirección (`nuevo→abierto→pendiente→resuelto→abierto`): cada transición debe disparar la notificación correspondiente sin error en logs.
- Filtros: `/tickets?view=resueltos`, `/tickets?view=pendientes`, `/tickets?view=todos_sin_resolver` — todos renderizan.
- Búsqueda: `/tickets?search=<algun_substring>` — renderiza.

- [ ] **Step 4: Tag intermedio**

```bash
git tag fase-c-completa
```

---

## Fase D — Eliminar `src/Utility/`

### Task 29: Verificación final + eliminar archivos legacy

**Files:**
- Delete: `src/Utility/SettingKeys.php`
- Delete: `src/Utility/ValidationConstants.php`
- Delete: `src/Utility/SettingsEncryptionTrait.php`
- Delete: directorio `src/Utility/`

- [ ] **Step 1: Cero referencias a `App\Utility\` en consumidores**

Run:
```bash
grep -rn "App\\\\Utility" src/ config/ templates/ webroot/ 2>&1 | grep -v "src/Utility/"
```

Expected: **cero** líneas. Si hay matches, son archivos que olvidamos migrar — regresar a Fase B y arreglar antes de continuar.

- [ ] **Step 2: Eliminar archivos**

```bash
git rm src/Utility/SettingKeys.php
git rm src/Utility/ValidationConstants.php
git rm src/Utility/SettingsEncryptionTrait.php
```

- [ ] **Step 3: Eliminar directorio vacío**

```bash
rmdir src/Utility
```

Si retorna error (`Directory not empty`), listar contenido con `ls -la src/Utility/` y revisar archivos ocultos antes de continuar.

- [ ] **Step 4: Regenerar autoload**

Run: `composer dump-autoload`
Expected: `Generated autoload files`.

- [ ] **Step 5: Coding standard**

Run: `composer cs-check && composer cs-fix`
Expected: cero errores. Si `cs-fix` modifica algo, hacer otro `git add -u`.

---

### Task 30: Smoke test final + commit Fase D

**Files:** ninguno.

- [ ] **Step 1: Smoke test final exhaustivo**

Manualmente probar:
- Login (admin, agent, requester).
- `/tickets` listado para los 3 roles.
- Crear ticket nuevo desde `/tickets/add`.
- Detalle de un ticket: agregar comentario público, agregar internal, cambiar estado.
- `/admin/settings`: leer y guardar configuración (n8n_enabled toggle).
- `/admin/email-templates`: listar y abrir un template.
- `bin/cake import_gmail --max 1` si Gmail está configurado.

Sin errores en `logs/error.log`.

- [ ] **Step 2: Verificar smoke checks del spec §5.4**

Run: `grep -rn "App\\\\Utility" src/ config/ templates/ webroot/ 2>&1`
Expected: **cero** resultados.

Run:
```bash
grep -rn -E "(en_progreso|convertido|cerrado)" src/ templates/ 2>&1 | grep -v 'cerrado sesión'
```
Expected: **cero** resultados.

Run:
```bash
grep -rn -E "(en_progreso|convertido|cerrado)" config/Migrations/ 2>&1
```
Expected: matches **solo** en:
- `config/Migrations/20260430213127_Initial.php` (esquema histórico).
- `config/Migrations/<timestamp>_ConsolidateLegacyTicketStatuses.php` (la migration de Fase B.5).

- [ ] **Step 3: Verificación BD**

Run:
```bash
mysql -h <DB_HOST> -u <DB_USERNAME> -p <DB_DATABASE> -e "SELECT status, COUNT(*) AS cnt FROM tickets GROUP BY status ORDER BY status;"
```

Expected: solo aparecen `nuevo`, `abierto`, `pendiente`, `resuelto`.

- [ ] **Step 4: Commit Fase D**

```bash
git status
```

Expected: 3 archivos eliminados de `src/Utility/`, posiblemente `composer.lock` si cambió.

```bash
git add -A
git commit -m "$(cat <<'EOF'
refactor(structure): remove src/Utility/ — replaced by src/Constants/

Phase D of Críticos 1-3 refactor. All consumers migrated, src/Utility/
folder deleted. The constants previously in ValidationConstants are now
distributed across TicketConstants/RoleConstants/CacheConstants.

Spec: docs/superpowers/specs/2026-05-07-criticos-1-3-design.md
EOF
)"
```

- [ ] **Step 5: Tag estable**

```bash
git tag criticos-1-3-implementado
```

---

## Sincronización de `CLAUDE.md`

### Task 31: Actualizar `CLAUDE.md` con la realidad post-refactor

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: Reemplazar bloque de `src/Controller/`**

Edit `CLAUDE.md`:

Find:
> `src/Controller/` — HTTP edge. Controllers stay slim and delegate to services. `TicketsController` composes behavior from controller traits in `src/Controller/Traits/` (`TicketSystemControllerTrait`, `TicketSystemListingTrait`, `TicketSystemViewTrait`, `TicketSystemActionsTrait`, `TicketSystemBulkTrait`, `TicketSystemHistoryTrait`, `ServiceInitializerTrait`, `ViewDataNormalizerTrait`). These traits still expose an `$entityType` parameter from when a second module existed; today only `'ticket'` is supported.

Replace with:
> `src/Controller/` — HTTP edge. `TicketsController` is currently a single ~1100-line file organized internally by `// region:` markers (Listing, View, Actions, Bulk, History). The original intent was to split these into traits under `src/Controller/Trait/`, but that extraction has not been done. The methods still expose an `$entityType` parameter from a removed second module — today only `'ticket'` is supported, and the parameter is dead abstraction pending removal. See `docs/audits/2026-05-07-architecture-audit.md` (Crítico 3.2 god-controller, Crítico 3.3 `$entityType`).

- [ ] **Step 2: Reemplazar bloque de Service Traits**

Find:
> Reusable mixin logic lives in `src/Service/Traits/` (e.g. `NotificationDispatcherTrait`, `GenericAttachmentTrait`, `TicketSystemTrait`, `ConfigResolutionTrait`, `SecureHttpTrait`).

Replace with:
> Reusable mixin logic lives in `src/Service/Traits/`: `ConfigResolutionTrait`, `GenericAttachmentTrait`, `SecureHttpTrait`, `SettingsEncryptionTrait` (consumed by `AppController` and `GmailImportService` for transparent encryption of sensitive `system_settings` keys).

- [ ] **Step 3: Agregar bloque de `src/Constants/`**

En la sección "Layered structure", agregar (preferiblemente justo antes de `src/Model/`):

```markdown
- **`src/Constants/`** — final classes con constantes de dominio. **Nunca hardcodear strings o IDs de dominio**; referenciar estas clases. Archivos:
  - `TicketConstants` — estados de ticket, prioridades, tipos de comentario, labels y colores de presentación.
  - `RoleConstants` — roles de usuario y atajo `STAFF_ROLES`.
  - `CacheConstants` — keys/configs de cache y `DEFAULT_SYSTEM_TITLE`.
  - `SettingKeys` — keys usadas en la tabla `system_settings`.
```

- [ ] **Step 4: Agregar entrada de "Ticket status enum" en Cross-cutting conventions**

En la sección "Cross-cutting conventions", agregar como nueva viñeta:

```markdown
- **Ticket status enum**: el modelo canónico de 4 estados (`nuevo`, `abierto`, `pendiente`, `resuelto`) vive en `TicketConstants::STATUSES`. Estados anteriores (`convertido`, `cerrado`, `en_progreso`) fueron consolidados a `resuelto` en la migration `ConsolidateLegacyTicketStatuses` (mayo 2026). Los helpers `StatusHelper::statusLabel/statusColor` mantienen un fallback genérico defensivo, pero ningún flujo de runtime debería producir valores fuera de `STATUSES`.
```

- [ ] **Step 5: Eliminar referencias residuales a `src/Utility/`**

Run: `grep -n "Utility\|ValidationConstants" CLAUDE.md`
Expected: cero líneas. Si aparece alguna, eliminar manualmente.

- [ ] **Step 6: Verificar coherencia**

Leer las secciones modificadas. Confirmar:
- No hay menciones de `TicketSystemControllerTrait` ni los otros 6 traits que no existen.
- No hay menciones de `NotificationDispatcherTrait` ni `TicketSystemTrait`.
- Sí hay mención de `src/Constants/` y `SettingsEncryptionTrait` en `Service/Traits/`.

- [ ] **Step 7: Commit**

```bash
git add CLAUDE.md
git commit -m "$(cat <<'EOF'
docs(claude): sync CLAUDE.md with post-refactor reality

Reflects state after Críticos 1-3 implementation:
- Removes references to non-existent controller traits
- Documents src/Constants/ as new home of domain constants
- Documents 4-state canonical ticket model and legacy consolidation
- Updates src/Service/Traits/ list to actual contents

Spec: docs/superpowers/specs/2026-05-07-criticos-1-3-design.md
EOF
)"
```

---

### Task 32: Verificación final del proyecto

**Files:** ninguno (solo verificación).

- [ ] **Step 1: Inspección visual de la app**

Hacer un recorrido visual completo:
- Home `/`.
- Login con cada rol.
- Listado de tickets — verificar sidebar contadores.
- Crear ticket nuevo, agregar comentario, cambiar estados.
- `/admin/settings` — guardar configuración Whatsapp/n8n.
- Detalle de un ticket que era `convertido` antes de la migration (ahora `resuelto`): debe renderizar sin warnings.

- [ ] **Step 2: Logs limpios**

Run: `tail -100 logs/error.log`
Expected: ninguna entrada nueva relacionada con `App\Utility`, `ValidationConstants`, ni `convertido`/`cerrado`/`en_progreso`. Solo errores no relacionados con este refactor (si los hay).

- [ ] **Step 3: Final commit limpio**

Run: `git status`
Expected: working tree clean.

Run: `git log --oneline pre-criticos-1-3..HEAD`
Expected: ~20+ commits ordenados (Fases A-D + CLAUDE.md). Cada uno con mensaje conventional-commits.

- [ ] **Step 4: Plan completo**

El refactor de Críticos 1-3 está implementado. Tags disponibles para rollback:
- `pre-criticos-1-3` — estado anterior.
- `fase-b-completa` — solo migración de namespaces sin tocar legacy.
- `fase-c-completa` — todo limpio excepto `src/Utility/`.
- `criticos-1-3-implementado` — estado final.

---

## Self-Review Checklist (resultado tras escribir el plan)

**Spec coverage** — cada sección del spec mapea a tasks:
- §1 Layout objetivo → Tasks 2-6
- §2 Contenido TicketConstants → Task 5 (con corrección de colores)
- §3 Fase A → Tasks 1-7
- §3 Fase B → Tasks 8-16
- §3 Fase B.5 → Tasks 17-19
- §3 Fase C → Tasks 20-28
- §3 Fase D → Tasks 29-30
- §4 Sync CLAUDE.md → Task 31
- §5 Validación/casos borde → integrado en smoke tests por task
- §6 Out of scope → respetado (no hay tasks que toquen Críticos 4-6 ni Altos)

**Placeholder scan** — sin TBDs, TODOs, "Add error handling", "Similar to Task N". Cada step muestra el código completo.

**Type consistency:**
- API de `StatusHelper` preservada (`statusLabel`/`statusColor`/`priorityLabel`/`priorityColor`).
- `TicketConstants::STATUSES` (no `STATUS_LIST`), `RESOLVED_STATUSES` (no `RESOLVED_STATUS_LIST`), `OPEN_STATUSES` consistente entre tasks.
- `RoleConstants::ROLE_*` consistente.
- `CacheConstants::CACHE_SETTINGS`/`CACHE_CONFIG` consistente.
- `App\Service\Traits\SettingsEncryptionTrait` namespace consistente.

**Issue conocido**: Tasks 8-15 dependen de inspección manual del archivo target para localizar imports — algunos archivos pueden no tener los imports listados (p. ej. WebhooksController solo usa SettingKeys, no ValidationConstants). El plan dice "si aparecen, reemplazar" — eso es interpretable. Asumiendo desarrollador con criterio, esto está OK.

---

**Plan complete and saved to `docs/superpowers/plans/2026-05-07-criticos-1-3.md`.**
